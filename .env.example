# Optional GitHub token (free) to increase rate limits
GITHUB_TOKEN=


# Free provider selection
LLM_PROVIDER=rule

# Optional: Hugging Face Inference (free-tier)
HUGGINGFACE_API_KEY=
HF_MODEL=Qwen/Qwen2.5-Coder-1.5B-Instruct

MAX_FILES=60
MAX_BYTES_PER_FILE=100000